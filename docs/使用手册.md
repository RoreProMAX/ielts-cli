# IELTS CLI 使用手册

这是跟打、默写和间隔复习的终端工具。通用入口的 V3 为 3.3.1-beta.1；各版独立保存记录。

## 启动与版本

| 平台 | 默认启动 | 带着进度移动 |
|---|---|---|
| Windows | 双击 `start.bat` | 在终端运行 `start.bat --portable` |
| macOS | 双击 `start.command` | 在终端运行 `sh start.command --portable` |
| Linux | 在终端运行 `sh start.sh` | `sh start.sh --portable` |

脚本支持相同参数。下文使用 `python3 launcher.py`，Windows 可替换为 `py -3 launcher.py`。

```bash
python3 launcher.py --app-version 1
python3 launcher.py --app-version 2
python3 launcher.py --app-version 3
python3 launcher.py --doctor
python3 launcher.py --verify
```

| 版本 | 能力 |
|---|---|
| V1 | 五本词库、跟打/默写、错词补练、到期复习、暂停续接 |
| V2 | V1 加英选中、中选英、听写、混合题型、例句与搭配 |
| V3 3.3.1-beta.1 | V2 加每日目标、每组 5 词、自动混合新词题型、增量例句返回单词学习、程序内提醒、后台版本检查与用户确认更新、可选 beta channel、Windows VT/native console 后端 |

## 按键

| 按键 | 功能 |
|---|---|
| Enter | 检查答案；答对后再次按下进入下一题 |
| Tab | 跟打 / 默写 |
| F2 / Ctrl+R | 当前词库的全部词 / 错词 |
| F3 / Ctrl+G | 显示答案并进入补练 |
| F4 / Ctrl+N | 跳过当前题 |
| F5 / Ctrl+P | 手动播放当前词读音 |
| F6 / Ctrl+T | 开关自动发音 |
| F7 / Ctrl+D | 选择词库 |
| F8 / Ctrl+K | 选择章节 |
| F9 / Ctrl+B | 到期复习 / 暂停返回学习 |
| Esc | 打开菜单；菜单按 3 再 Enter 选择题型，按 10 进入版本与更新（V3；内部选项编号随状态动态显示） |
| F11 / Ctrl+Y | 今日任务（V3） |
| F12 / Ctrl+L | 目标与提醒设置（V3） |
| Ctrl+S | 复习提醒稍后 15 分钟（V3） |
| F1 | 帮助 |
| Ctrl+Q / Ctrl+C | 保存退出 |

功能键被系统占用时使用对应 Ctrl 快捷键。V2 历史版保留 F10/Ctrl+O；V3 使用 Esc 菜单，输入为空时按 `?` 也可打开菜单。普通字母 `q` 用于拼写。界面至少需要 40 列、6 行；阅读长选项时建议增大窗口。

选择器支持上下键、翻页、直接输入编号、Enter 确认及 Esc 返回。输入支持左右、Home/End、Backspace/Delete 和 Ctrl+U 清空。

## 词库

| 词库 | 有效词条 | 章节 | ID |
|---|---:|---:|---|
| 雅思通用 | 3575 | 179 | `ielts` |
| 四级 | 2607 | 131 | `cet4` |
| 六级 | 2345 | 118 | `cet6` |
| 雅思听力场景 | 1171 | 59 | `ielts-listening` |
| 雅思扩展 | 9388 | 470 | `ielts-expanded` |

每章 20 词，末章按实际数量显示。不同词库各自保留章节、输入、错词与次数，相同拼写在不同词库中分别统计。

```bash
python3 launcher.py --list-dictionaries
python3 launcher.py --dictionary ielts-listening --chapter 3
```

## 学习与复习

启动时有到期词会优先复习，复习默认默写。想继续新词可加 `--learn`；手动进入到期复习使用 F9 或 `--review`。

首次跟打后 10 分钟进入复习；无提示的拼写答对按 1、3、7、14、30 天推进。错误和提示后 10 分钟重测，跟打或选择题答对不会延长拼写复习间隔。

错答后对照答案完整跟打一遍；后面至少还有三题时插入延后重测，同轮最多重试两次。未完成题和输入可保存续接。复习覆盖各词库，顶部显示当前题所属词库。

```bash
python3 launcher.py --learn
python3 launcher.py --review
python3 launcher.py --review-intervals 1,3,7,14,30
python3 launcher.py --stats
```

## 题型

V3 学习中按 Esc 打开菜单，按 3 再 Enter 选择题型；V2 历史版使用 F10/Ctrl+O。学习和复习分别保存设置。

| 模式 | 参数 |
|---|---|
| 跟打 | `copy` |
| 中文提示默写 | `recall` |
| 英选中 | `en_to_zh` |
| 中选英 | `zh_to_en` |
| 听音拼写 | `listening` |
| 例句填空 | `cloze` |
| 常用搭配 | `collocation` |
| 按成绩选择题型 | `mixed` |

```bash
python3 launcher.py --learn --question-mode mixed
python3 launcher.py --review --question-mode mixed
```

选择题直接按 1–4 作答。中选英和默写显示词库中全部已有释义，保留词性及义项标点；长释义自动换行，窗口较小时可用上下键或 PageUp/PageDown 翻阅，选项保持可见。V3 新词默认 mixed，每组 5 词按跟打、英选中、中选英、默写四阶段推进，阶段之间自动穿插；组末自动接下一组。每日任务完成后回到每日面板。复习默认 recall，完成一组到期复习后自动接学习。各题型成绩分别记录。

例句和搭配目前覆盖 124 个词。直接选择这两种模式时使用已有内容的词；复习遇到没有配套内容的词时提示并回退到默写。菜单 9 进入增量例句后，菜单 2 可返回单词学习并保留当前题目、输入和评分上下文。

## 每日任务

默认每日新词 20、复习 30，每组 5 词。F11 打开面板，按 1 开始下一组，先处理到期复习再从当前词库选新词。每日目标按不同词条计数，表示练习量，不代表掌握率。

每日下一组跳过今天已复习过的词，避免选择题重复占满小组。错词同轮重测保留，需要继续重测当天到期词可用 F9。已有未完成轮次优先续接。

```bash
python3 launcher.py --daily
python3 launcher.py --configure --new-goal 20 --review-goal 30 --batch-size 5
python3 launcher.py --configure --reminder-minutes 30
```

程序内提醒只显示提示，不替换当前题或输入；Ctrl+S 延后 15 分钟。设置间隔为 0 可关闭程序内提醒。Linux 退出后的后台提醒安装和迁移见部署指南；默认不启用。

## 发音与保存

自动发音默认关闭，F5 可手动播放，按 F6 开启自动发音后可自动播放跟打和英选中当前词。例句和搭配支持增量与学习中穿插；默写、中选英和填空答题阶段不会自动读答案，这些题手动播放会记为使用提示。

发音需要 `ffplay`。首次获取某词读音需要联网，成功下载后保存在本地缓存。没有发音依赖或网络时仍可继续文字练习。

输入停顿后自动保存，提交和退出时立即保存；同一数据目录只允许一个学习终端。分享入口不自动导入其它程序或旧版本记录。数据位置和迁移步骤见部署指南。

## 增量练习与学习中例句

新词目标完成后，程序接上剩余的到期复习；达到复习目标，或当天实际到期词已处理完，就默认进入增量练习。到期词少于配额时按实际到期量计算。每组按设置的词数安排例句或搭配，自动交替，使用已学且有配套内容的词。增量分开统计，不充当新词/复习目标，答对不会把原复习间隔连续推远。

已回答的词/题型当天不会重新加入下一增量组；错题仍可补练和延后重测。当天可用内容练完或跳过后回到任务面板，随时可以保存退出。

Esc 菜单第 9 项可手动进入增量练习。Esc 菜单进入“学习计划”后，第 7 项是“每日任务完成后增量练习”（默认开）；第 8 项是“学习过程中加入例句”（默认关）。开关对下一组生效，当前题和输入保留。开启穿插后，有材料的词会在跟打、英选中、中选英、默写之后再练例句和搭配。

```bash
python3 launcher.py --incremental
python3 launcher.py --configure --extra-after-daily on
python3 launcher.py --configure --examples-during-learning on
```

把 on 换成 off 即可关闭。例句显示英文填空、中文句意和目标词义；答题后可查看完整句子，长文本用上下键翻阅。当前有 124 个词的配套例句及搭配，覆盖雅思通用前 40 词和其它词库的部分初始词汇，尚未覆盖所有词条。

## 版本与更新

自动更新从 V3.3.0 开始提供。V3.3.0 及更早版本没有 Beta 入口，首次需手动安装 V3.3.1-beta.1；不要求先安装中间版本。V3.3.1-beta.1 启动时默认后台检查公开 stable Release；channel 默认 stable，可在 Esc 菜单 10“版本与更新”切换 beta。稳定用户不会收到 Beta；Beta 只接受高于当前版本的 stable/beta，不降级。检查不下载、不安装、不重启，也不占用当前题目；学习计划中明确提供“启动时检查更新”开关，默认开启。

更新页先显示“下载并校验 vX”；下载完成后由用户选择“保存进度并重启到 vX”或“下次启动使用 vX”。Beta 与 stable 数据默认按版本隔离，不双向同步；自动更新沿用当前同一进度目录，和手工复制到新版本目录的跨版本迁移不同。`--no-update` 仅本次使用原入口版本并跳过检查。更新不会升级 Python、windows-curses、ffplay 或词库，也不会上传个人词库和学习进度。

命令行可用 `--configure --auto-update-check on|off` 配置、`--update-status` 只读查看状态；`--no-update` 仅本次使用原入口版本并跳过更新检查，不修改永久开关。测试或需要完全隔离网络时设置 `IELTS_DISABLE_UPDATE_CHECK=1`。

切换更新通道使用 `--configure --update-channel stable|beta`；默认是 `stable`。Beta 与 stable 版本按目录隔离，启用 channel 不会自动下载或激活。
